" Try to use Ctrl-[ instead of Escape
" Try to use Ctrl-h instead of Backspace
" ins + Ctrl-r is handy (insert contents of register)!
" Ctrl-t / Ctrl-d are handy (add/remove indent from start of line, typed from anywhere in the line)!
" Learn Ctrl-x commands better
" Objects: 
"  * a" - double quoted string
"  * a' - single quoted string
"  * i" - double quoted string sans-quotes
"  * i' - single quoted string sans-quotes
